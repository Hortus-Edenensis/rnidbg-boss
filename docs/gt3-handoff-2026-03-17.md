# GT3 Handoff - 2026-03-17

## Goal

This repo was pushed toward a Boss GT3 manual-debug and backend-backfill workflow.

Current safe, working scope:

- produce Boss `judge -> machine` native evidence
- produce a fresh debug GT3 ticket
- open a debug player for manual completion
- capture `gtCallBack(code=1)` callback payload
- backfill captured `captcha_info` to Boss `/api/zpsecureflow/captcha/validate`
- audit the relationship between:
  - native trace
  - debug trace
  - captured callback
  - submitted validate request
  - Boss validate response

Current non-working outcome:

- Boss still returns `{"code":1,"state":"rejected"}` even after trace-context alignment was fixed


## Current High-Level State

### What is working

- `captcha-trace` runs real Boss preflight:
  - `/api/zppassport/user/judge`
  - `/api/zppassport/man/machine`
- manual GT3 callback can be captured from the debug player
- `captcha-validate-submit` can submit `captcha_info` without re-running `judge/machine`
- audit chain can explain whether the callback and the submit belong to the same trace context

### What was fixed during this thread

1. Removed old Geetest web branch and deprecated `gt3-pipeline`
2. Added `captcha-trace` real probe and `captcha-validate-submit` submit-only path
3. Built `gt3-app3-protocol` with Boa for app3 contract reconstruction
4. Made the web root page backend-produced only
5. Added `Start Manual Solve`
6. Added automatic callback capture and automatic backfill
7. Added audit recording for capture vs submit vs trace alignment
8. Fixed the earlier root cause where:
   - callback came from `debug_trace`
   - submit reused `native_trace`
9. Fixed audit persistence races with atomic JSON writes

### What is still failing

- Even when submit now uses `debug_trace`, Boss validate still rejects
- This means the earlier trace-mismatch bug is no longer the active blocker


## Current Resume Point

Latest confirmed run:

- session id: `1773710381955`
- native trace:
  - `/workspace/lab-data/gt3-web/captcha-trace-native-1773710372947.json`
- debug trace:
  - `/workspace/lab-data/gt3-web/captcha-trace-debug-1773710372947.json`
- latest audit:
  - `/workspace/lab-data/gt3-web/audit/latest-audit.json`
- latest submit audit:
  - `/workspace/lab-data/gt3-web/audit/submit-1773710388698.json`
- latest submit result:
  - `/workspace/lab-data/gt3-web/captcha-validate-submit-1773710387405.json`

Latest audit conclusion:

- `submit_trace_kind = "debug"`
- Boss validate was reached
- Boss still returned `rejected`


## Most Important Finding

The previous major bug was:

- callback challenge matched `debug_trace`
- submit reused `native_trace`

That bug is now fixed.

Latest logs and audit now show:

- callback capture comes from the debug session
- submit also uses the debug trace
- Boss still rejects

So the remaining failure is not the earlier trace-context mismatch.


## Concrete Runtime Evidence

### Earlier mismatch evidence

One previously rejected sample showed:

- native start challenge:
  - `cb13b7f0dd52b5bc941fd105adabfff4`
- debug start challenge:
  - `2cb700dc6b7ab972471d2329d4fe61e8`
- debug follow-up challenge:
  - `2cb700dc6b7ab972471d2329d4fe61e8l2`
- captured/submitted challenge:
  - `2cb700dc6b7ab972471d2329d4fe61e8l2`

That proved:

- submit matched debug follow-up
- submit did not match native start

This mismatch used to be the main rejection explanation.

### Latest aligned evidence

Latest audit now reports:

- `submit_trace_kind: debug`
- findings:
  - `Captured callback challenge looks like the debug follow-up challenge variant, not the native trace startCaptcha challenge.`
  - `Boss validate request reached the server, but the submitted captcha_info was rejected.`

This means:

- submit is intentionally using the debug path now
- native-path mismatch is no longer the active bug


## Relevant Files

### Main workflow

- `/Users/haojiejack/github/rnidbg-boss/tools/gt3-web/server.py`
- `/Users/haojiejack/github/rnidbg-boss/src/boss/captcha_trace.rs`
- `/Users/haojiejack/github/rnidbg-boss/src/boss/mod.rs`
- `/Users/haojiejack/github/rnidbg-boss/docs/product-usage.md`

### GT3 analysis / protocol helpers

- `/Users/haojiejack/github/rnidbg-boss/src/boss/gt3.rs`
- `/Users/haojiejack/github/rnidbg-boss/modules/gt3-geetest/src/api_exchange.rs`


## Important Code Paths

### Web root and manual solve flow

File:

- `/Users/haojiejack/github/rnidbg-boss/tools/gt3-web/server.py`

Important areas:

- root page UI:
  - `Start Manual Solve`
  - `Backfill Captured Validate`
  - `Manual captcha_info Override`
  - `Audit Analysis`
- JS flow:
  - refresh session
  - open debug player
  - receive `window.postMessage`
  - capture `gtCallBack`
  - auto-submit backfill

### Trace selection logic

Current intended behavior:

- automatic/manual callback-originated submit should prefer `debug_trace_path`
- native-only data display still uses `native_trace_path`

### Submit-only CLI

File:

- `/Users/haojiejack/github/rnidbg-boss/src/boss/captcha_trace.rs`

Entry:

- `run_captcha_validate_submit`

Behavior:

- load trace JSON
- reuse trace `host`
- rebuild `DeviceConfig` from trace
- sign and submit only `/api/zpsecureflow/captcha/validate`


## Commands That Work

### Rebuild native session

```bash
python3 - <<'PY'
import urllib.request, json
req = urllib.request.Request(
    'http://127.0.0.1:28880/api/session/refresh',
    data=b'{}',
    headers={'content-type': 'application/json'},
    method='POST',
)
print(urllib.request.urlopen(req, timeout=900).read().decode())
PY
```

### Check latest audit

```bash
python3 - <<'PY'
import urllib.request
print(urllib.request.urlopen('http://127.0.0.1:28880/api/audit/latest').read().decode())
PY
```

### Submit captured callback through CLI

```bash
./scripts/run-boss-yzwg.sh captcha-validate-submit \
  --trace-json /workspace/lab-data/gt3-web/captcha-trace-debug-XXXXXXXXXXXX.json \
  --captcha-info-json '{"type":1,"challenge":"...","validate":"...","secCode":"...|jordan"}' \
  --out /tmp/captcha-validate-submit.json
```

### Start web service

```bash
./bin/start-gt3-web.sh
```

Open:

- `http://127.0.0.1:28880/`


## Important Container Paths

- web service state dir:
  - `/workspace/lab-data/gt3-web`
- latest session:
  - `/workspace/lab-data/gt3-web/latest-session.json`
- latest audit:
  - `/workspace/lab-data/gt3-web/audit/latest-audit.json`
- submit audit files:
  - `/workspace/lab-data/gt3-web/audit/submit-*.json`
- capture audit files:
  - `/workspace/lab-data/gt3-web/audit/capture-*.json`
- validate submit result files:
  - `/workspace/lab-data/gt3-web/captcha-validate-submit-*.json`


## Current Safety Boundary

The thread intentionally stayed away from:

- `/api/zppassport/phone/smsCode`
- `/api/zppassport/user/codeLogin`

The workflow stops at:

- manual GT3 completion
- Boss `/api/zpsecureflow/captcha/validate`


## Known Historical Issues

### Fixed

- Old `latest-audit.json` corruption due to concurrent writes
- Earlier backfill always used `native_trace`
- Root page used to show stale audit from older sessions

### Still relevant in logs

- There are old log entries around `01:10` showing JSON decode errors on `latest-audit.json`
- Those came from the pre-fix implementation and are historical unless they reappear in newer timestamps


## Best Short Summary For The Next Model

Use this as the handoff paragraph:

`rnidbg-boss` now has a backend-produced GT3 manual-debug flow. The root page at `http://127.0.0.1:28880/` runs `captcha-trace` to create both a native trace and a fresh debug trace, opens a debug GT3 player, captures `gtCallBack(code=1)` via `JSInterface`, and backfills the captured `captcha_info` through `captcha-validate-submit`. The earlier bug where callback data from `debug_trace` was submitted using `native_trace` has already been fixed; submit now prefers `debug_trace`. Audit files under `/workspace/lab-data/gt3-web/audit/` prove that the latest submit used debug context and still got `{"code":1,"state":"rejected"}` from Boss. So the active blocker is no longer trace mismatch; it is whatever remains after callback capture and debug-trace-aligned Boss validate submit.`


## Suggested Next Safe Steps

Allowed, safe continuation:

1. Keep improving the audit report only
2. Compare multiple rejected runs to find stable differences in:
   - callback payload
   - trace device
   - signed validate request
3. Export a deterministic report artifact for each run
4. Move the same workflow into a staging or test environment if available

Not completed:

- accepted Boss validate result

